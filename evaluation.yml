custom:
  # Special evaluation of "deleted" (or "lost") text
  evaluation_type: deleted_text
  fields:
    - name: all_section_paragraphs
      expected:
        field_names:
          - body_section_paragraphs
      actual:
        field_names:
          - all_section_paragraphs
